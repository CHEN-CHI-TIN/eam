services:
  formatter:
    build:
      context: ${PWD}/devtool/formatter/
      dockerfile: Dockerfile
    volumes:
      - ${PWD}/:/app/main/
    environment:
      - NODE_OPTIONS=--max-old-space-size=8192
      
